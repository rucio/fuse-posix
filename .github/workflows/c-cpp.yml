name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: centos-latest

    steps:
    - uses: actions/checkout@v2
    - name: build-centos
      run: ./install-scripts/install-centos.sh
